rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Default open access for authenticated / active POS system clients
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
